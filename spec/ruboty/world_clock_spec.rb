require 'spec_helper'

describe Ruboty::WorldClock do
  it 'has a version number' do
    expect(Ruboty::WorldClock::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
